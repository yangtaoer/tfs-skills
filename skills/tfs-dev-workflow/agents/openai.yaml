interface:
  display_name: "TFS Dev Workflow"
  short_description: "Pull TFS stories, develop locally, and submit PRs"
  default_prompt: "Use $tfs-dev-workflow to pull a reviewed TFS user story, confirm local repo settings and target branch, implement it, and submit PRs."
