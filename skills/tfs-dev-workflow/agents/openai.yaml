interface:
  display_name: "TFS Dev Workflow"
  short_description: "Pull TFS stories, develop locally, and submit PRs"
  default_prompt: "Use $tfs-dev-workflow to pull a reviewed TFS user story, prepare local repo branches, implement it, and submit PRs to dev."
