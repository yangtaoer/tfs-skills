interface:
  display_name: "TFS Run Pipeline"
  short_description: "Run staged TFS delivery workflows and return artifacts"
  default_prompt: "Use $tfs-run-pipeline to run the complete cataloged delivery workflow for a resolved TFS project, respect pipeline stages, wait for completion, and return its artifact links."
